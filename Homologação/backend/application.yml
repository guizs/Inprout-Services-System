spring:
  #datasource:
   # url: ${DB_URL:jdbc:postgresql://localhost:5432/inpdatabase_v1}
    #username: ${DB_USER:postgres}
    #password: ${DB_PASS:inpservices123}
    #driver-class-name: org.postgresql.Driver

  datasource:
    url: ${DB_URL:jdbc:postgresql://3.128.248.3:5432/inprout.service-homolog}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:inpservices123}
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:bWV1cHJvamV0b2lucHJvdXRzZWd1cmFuY2EyMDI1Y29tY2hhdmVzdXBlcmZvcnRl}